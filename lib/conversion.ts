// Conversion plumbing — paste the two strings below and the demo page
// upgrades itself. Nothing here ships a broken promise: with both empty, the
// booking card degrades to a same-day-reply email form via mailto.

// Google Calendar appointment schedule embed URL.
// Get it: calendar.google.com → your appointment schedule → Share → Embed →
// copy the iframe src (looks like
// "https://calendar.google.com/calendar/appointments/schedules/…?gv=true").
export const CALENDAR_EMBED_URL = "";

// Loops form endpoint for email capture + nurture.
// Get it: app.loops.so → Forms → your form → endpoint URL (looks like
// "https://app.loops.so/api/newsletter-form/clxxxxxxxxx").
export const LOOPS_FORM_ENDPOINT = "";

export const DEMO_EMAIL = "demo@aleq.com";
